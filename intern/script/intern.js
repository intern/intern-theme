(function(c) {
  c.replace('h5',{hover: true}).replace('#navigation a', {hover: true}).replace('#header h2', {hover: true}).replace('.region-content h2', {hover: {textShadow: '1px 1px #000'}});
})(Cufon);
